For Fast Api need to install fastapi and unicorn for server

// pip  install fastapi uvicorn

#1 
For run server
// uvicorn main:app --reload

#2
Crete env 
1. python -m venv venv
2. .\venv\Scripts\Activate.ps1

// you will get on terminal (venv)

#3 JWT with Auth
we need to install 
1. python-jose
2. "passlib[brycpt]"
3. python-multiper

#3 
Cashing needs to install 
1. BeautifulSoup
2. requests

#4
CRUD_Fast_DB
needs to install

1. sqlalchemy

#5
Rate limiting
needs to install

1. slowapi // for nodejs we use "express-rate-limit"

#6
We can deploying you code using "Render" 